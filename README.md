# ОРГАНИЗОВАННАЯ КУЛЬТУРНОСТЬ — сайт

**Версия: `orgculture-v48`**
**Обновлён:** 25.08.2026

## Правило версий

При каждой партии изменений сайта обязательно:
1. Поднять номер версии выше
2. Обновить дату
3. Дописать строку в «Историю изменений» — что именно поменялось и почему
4. Обновить «Что осталось доделать», если что-то закрылось или появилось

Версия архива (`orgculture-archive-vN`) считается отдельно — см. README
в корне полного архива.

## SITE_VERSION — держать в синхроне с версией сайта

В `gen.py` есть отдельная константа `SITE_VERSION` — она отвечает за
кэш-бастинг `style.css?v=N` и за номер кэша в `sw.js`. **Бампать её
вместе с версией из этого README при каждой правке**, иначе вернувшиеся
пользователи будут долго видеть старые стили (service worker кэширует
статику по стратегии cache-first).

## История изменений

**v48** — исправлено то, что в v47 было сделано вхолостую, плюс
сокращение текста.
- **Настоящая причина, почему кнопки уходили за экран:** блок
  `@media(max-width:700px)` из v47 стоял в файле ВЫШЕ базовых
  `.hero`-правил. Media-запрос не повышает специфичность, поэтому
  базовые правила, идущие ниже, молча перебивали его — уплотнение не
  работало вообще, хотя код был на месте. Блок перенесён ПОСЛЕ базовых
  правил (в самом CSS оставлен комментарий с объяснением, чтобы не
  повторить), значения ужаты сильнее: знак 220→150px, отступы hero
  96/64→48/40.
- **Лид-абзац сокращён.** «Пространство для рождения смыслов и новых
  значений» сохранено полностью, дальше короче: «Тексты о культуре —
  не чтобы порекомендовать, а чтобы отрефлексировать. Рядом —
  продюсирование и продвижение арт-проектов.» Синхронно в 4 местах
  (gen.py, gen_en.py, оба дефолта в 04-bot/worker.js).
- Проверено настоящим Chrome с мобильным viewport (390×844): знак
  реально 150px (значит media применился), нижний край второй кнопки —
  567px при экране 844px, обе кнопки помещаются с запасом.

**v47** — по скриншоту с телефона: два замечания на главной.
- **Текст не упоминал продюсирование/продвижение.** Лид-абзац
  дополнен фразой «Рядом — продюсирование и продвижение
  арт-проектов.» (RU) / «Alongside — production and promotion for
  arts projects.» (EN). Правка синхронизирована в трёх местах —
  gen.py, gen_en.py, дефолты `home_lede`/`home_lede_en` в
  04-bot/worker.js — проверено скриптом, не на глаз.
- **Кнопка «Продюсирование и продвижение» требовала скролла.** Новый
  `@media(max-width:700px)` блок уплотняет вертикальные отступы hero
  только на мобильном — десктоп не тронут вообще. Проверено честным
  mobile-viewport рендером через Puppeteer (wkhtmltoimage оказался не
  показывать @media по ширине корректно — сначала дал ложную картину,
  поймал и переключился на Chrome).
- **Находка по ходу:** при рендере через Puppeteer словомарка
  («ОРГАНИЗОВАННАЯ КУЛЬТУРНОСТЬ») получилась маленькой и в одну
  строку — заметно меньше, чем на присланном скриншоте (там она в два
  раза больше, на две строки). Возможно, на живом сайте сейчас другая,
  более старая версия — стоит проверить после деплоя этой версии, что
  видно на самом деле.

**v46** — редактор текста сайта («админка»), по образцу
`_tools/AdminPanel` из пака BALASHOV:
- Новый генерируемый `site-content.js` (не статический, в отличие от
  оригинала у BALASHOV — адрес воркера берётся из общего
  `WORKER_BASE`, второго места для ручной правки нет). Подключён на
  6 страницах: главная, `/production/`, `/about/` — RU и EN.
- 12 текстов помечены `data-editable` и правятся из веб-редактора без
  пересборки сайта: слоган и лид на главной, H1 и два абзаца на
  «Продюсирование», первый абзац био на «О себе» (всё в двух языках).
  Сами тексты-эссе и манифест сознательно НЕ выведены — короткое поле
  легко сломать, а последствия для длинного текста заметнее.
- Честный откат: редактор недоступен, пуст или ещё не настроен —
  посетитель молча видит исходный статический текст. Проверено в
  реальном DOM (jsdom) по всем трём сценариям, не только «код есть».
- **Найден и исправлен реальный баг:** EN-версия `/en/about/` всё ещё
  говорила «run social media» — та же известная регрессия, что чинили
  в RU-версии, боте и мета-описании, но здесь уцелела (шаблон в
  `KNOWN_BAD_PATTERNS` её не покрывал). Исправлено на «produce and
  promote», в `check_archive.py` добавлен паттерн на этот случай.

**v45** — публичное КП на сайте: кнопка «Скачать КП (PDF)» на
`/production/` и «Download the offer (PDF)» на `/en/production/`,
ведёт на новый `documents/orgculture-uslugi-i-ceny.pdf` (RU) /
`documents/orgculture-services-and-prices.pdf` (EN) — полный список
услуг, цены и условия сотрудничества, без адресата, для любого, кто
зашёл на сайт. Источники — `07-documents/commercial-offers/
site-general/` и `site-general-en/`, подробности там же в README.

**v44** — улучшения по собственному осмотру (не по чужому списку):
- **На главной не было `<h1>`** (RU и EN) — название сайта там SVG-словомарка, страница начиналась сразу с `<h2>` «Последние тексты». Поисковики и скринридеры не видели главного заголовка. Добавлен визуально скрытый `<h1>` (новый класс `.visually-hidden`), внешний вид не изменился — проверено скриншотом.
- **Шрифты: убраны неиспользуемые начертания** из запроса к Google Fonts. Запрашивались Unbounded 200/400 и Manrope 200/600 — ни одно из них не встречается ни в CSS, ни в инлайн-стилях, ни в собранном HTML (проверено разбором всех `font-weight`). Осталось ровно то, что используется: Unbounded 300/500, Manrope 300/400/500 — меньше вес загрузки на каждом заходе.

**v43** — отзывы без JavaScript (по образцу pack-v293 AELITA). Без JS человек видел кнопку «Оставить отзыв», которая ничего не делает (форму открывает скрипт), и пустой блок отзывов — тупик без единого объяснения. Добавлен `<noscript>`: нерабочая кнопка скрывается, появляется текст с объяснением и указанием на кнопку «через Telegram», которая работает без JS. RU+EN. Проверено эмпирически в jsdom без выполнения скриптов, не по факту «код добавлен».

**v42** — по итогам обзора run_audit.py в паке AELITA: самопроверка декодирования QR (generate_qr.py теперь сам расшифровывает результат и сверяет с ожидаемым URL), список известных регрессий, проверка обоих трекинг-сниппетов на каждой странице, валидность JSON-LD — всё в check_archive.py. По ходу поймано 3 реальных бага: текст бота (RU+EN) и мета-описание /en/about/ всё ещё говорили "SMM" вместо "продвижение" — не были обновлены вместе с сайтом ранее.

**v41** — маячок статистики теперь читает UTM-метки из URL
(`utm_source`/`utm_medium`/`utm_campaign`/`utm_content`) — раньше
брал только `location.pathname`, параметры из `location.search`
отбрасывались молча. Обнаружено при заведении правила «любой QR-код —
с UTM-разметкой» (новая папка `10-qr-codes/` в корне архива, см. её
README) — без этой правки разметка была бы декоративной, никак не
попадая в статистику. Воркер (`04-bot/worker.js`) считает
UTM-переходы отдельным ключом (`utm:<неделя>:<source>|<medium>|
<campaign>`), еженедельный отчёт получил новый раздел «Размеченные
переходы». Проверено прогоном логики воркера с подменённым KV —
счётчики пишутся верно, раздел в отчёте формируется корректно.

**v40** — целевой поиск технических багов (не сравнением с AELITA, а
прямым разбором кода) по твоей просьбе. Нашлись 4 реальных бага, все
исправлены:

- **`check_en.py` сам ломал то, что должен проверять** — внутри себя
  дёргает `runpy.run_path('gen.py')`, чтобы получить `TEXTS_RU` для
  сравнения, и это тихо перезаписывало `sitemap.xml`/`sw.js` RU-only
  версией (без hreflang, без `/en/` в precache). Если запустить
  `check_en.py` ПОСЛЕ `gen_en.py` — а именно такой порядок в чек-листе
  «Проверка после правок» ниже — регресс проходил незамеченным: сама
  проверка 4c сравнивала `ru_count==en_count`, а 0==0 тоже «совпадает».
  **Это уже было в отданном `orgculture-archive-v39.zip`.** Исправлено:
  `check_en.py` теперь сам восстанавливает EN-слой (`runpy.run_path('gen_en.py')`
  сразу после), плюс проверка 4c больше не считает 0/0 чистым результатом,
  плюс новая проверка 4e — `/en/` должен быть в `sw.js`.
- **Markdown в подтверждении отзыва (бот)** — `send()` всегда шлёт с
  `parse_mode:'Markdown'`; текст отзыва пользователя, вставленный туда
  как есть, при нечётном числе `*`/`_`/`` ` ``/`[` валил всю отправку
  без единого сообщения человеку. Экранируется перед показом,
  разэкранируется перед пересылкой админу (см. `04-bot/README.md`).
- **Имя с реальной скобкой обрезалось** — `extractReviewMeta` резал имя
  по первой `(`, что задумывалось только для `(@username)`. Регулярка
  сужена именно под этот формат.
- **Гонка при записи в KV** (`publishReview`/`queueForGithub`) —
  задокументирована как осознанный, не устранённый маленьким патчем
  риск (KV не даёт CAS, нужны Durable Objects) — раздел «Незакрытые
  задачи» в корневом README.

`SITE_VERSION` 39→40 (для консистентности с правилом «любая правка —
новая версия», хотя видимый HTML не менялся — только генераторы/чекер).

Проверено: `gen.py`→`gen_en.py`→`check_en.py` в правильном порядке —
EN-слой (`hreflang`, `/en/` в `sw.js`) подтверждён после каждого шага,
не только сразу после сборки; `node -c` на `worker.js`; ручной тест
экранирования на 4 кейсах (спецсимволы, скобки) — round-trip совпадает
с оригиналом; регулярка имени проверена на реальном имени со скобкой.

**v39** — по вашей правке из «Скоро понадобится обновить» (см. README
архива, раздел «Незакрытые задачи»): БуФест прошёл (10–15 августа),
сегодня 25.08 — даты на странице «Автор» и в CV читались бы как
заброшенный сайт. Убраны конкретные даты у обоих упоминаний:
- «Автор» (RU+EN) и CV (PDF+PPTX) — БуФест: было «(10–15 августа)» в
  буллете CV, стало «(август 2026)», без диапазона дней.
- «Принцесса цирка» — было «(август и октябрь 2026)», убрано вовсе:
  по вашей формулировке спектакль в прокате, не разовое событие с
  конкретными датами, которые сами по себе устаревают.

Правка задела **три независимые копии одного и того же текста** —
нашлось по пути, не было очевидно заранее: `gen.py` (сайт + CV_ROLES,
из которого через regex тянет `gen_cv_pdf.py`), `gen_en.py` (отдельная
EN-копия) и `07-documents/source-cv-pptx/gen_cv_pptx.js` (третья,
полностью самостоятельная копия текста, НЕ подключённая ни к `gen.py`,
ни к `check_archive.py` — тот же класс риска, что уже разбирался в
`v37`/`v38` на примере AELITA, просто здесь не найденный сравнением, а
всплывший от прямой правки). Все три поправлены и пересобраны заново;
`07-documents/CV-Konstantin-Moshnikov.pdf`/`.pptx` — синхронизированы
с версией из `03-website/`, `pptx`-скилл подтвердил валидность.

`SITE_VERSION` 38→39. Проверено: `gen.py`/`gen_en.py`/`gen_cv_pdf.py` —
без ошибок; `pdftotext` на пересобранном CV подтвердил отсутствие
старых дат в тексте; `python-pptx` — то же самое для pptx;
`check_en.py` — чисто; `check_archive.py` — чисто (7/7).

**v38** — по итогам обзора пака AELITA (pack-v150→v167, продолжение обзора
v35, который останавливался на pack-v149): точечная правка, взято одно
реальное совпадение. `pack-v154` AELITA — раньше почти везде было
пассивное «нажимая кнопку, вы соглашаетесь…» рядом с кнопкой, что не
является «конкретным, информированным и однозначным действием», как
того требует ст. 9 152-ФЗ; заменено на настоящий чекбокс, не отмеченный
по умолчанию, блокирующий отправку. Проверено, что тот же пробел есть и
здесь (не предположено): форма отзыва на странице текста
(`review-form` в `gen.py`/`gen_en.py`) не имела вообще никакого
упоминания согласия; диалог отзыва в боте пересылал текст на модерацию
сразу же, без отдельного шага подтверждения (см. `04-bot/README.md`).

- **Сайт (RU+EN, 76 страниц текстов)** — в форму отзыва добавлен
  непредзаполненный чекбокс согласия со ссылкой на `/privacy/` (EN:
  `/en/privacy/`), блокирующий кнопку «Отправить»/«Send», пока не
  отмечен (проверка и на `disabled`, и повторно внутри обработчика
  клика). Текст и ссылка сверстаны ОДНИМ `<span>` внутри
  flex-`<label>` — намеренно, не отдельными flex-элементами: та же
  разметка, что уже ловила баг с переносом текста у AELITA
  (`.cb-item{display:flex}` разбивал текстовые узлы построчно), здесь
  просто не даёт этому классу бага завестись.
- Новый CSS-класс `.review-form-consent` в `assets/style.css`.
- `SITE_VERSION` 37→38 (только этим и обновляется — `gen.py`/`gen_en.py`
  пересобраны целиком, `sw.js` и кэш-бастинг стилей подтянулись
  автоматически).

Не перенесено из этого диапазона AELITA: билеты/оплата/личный
кабинет/тестовые режимы/схемы залов/рассылка — специфика продакшена
как бизнеса, у orgculture ничего из этой инфраструктуры нет. Отдельно
проверено (не пропущено) и явно НЕ применимо: баг PDF-генератора
`<ul><li>`/`⚠️` (`pack-v167`) — orgculture рендерит легальные PDF через
`wkhtmltopdf` (настоящий движок), а не самописный HTML→PDF парсер, как
у AELITA, тот класс бага физически не воспроизводится; аудит
синхронизации футера/меню — по-прежнему неприменим, см. `v35`.

Проверено: `python3 gen.py`/`gen_en.py` — без ошибок, 76 текстовых
страниц (RU+EN) распарсены `lxml` — валидны; `check_en.py` — чисто
(0 по всем четырём категориям); чекбокс и ссылка на privacy проверены
построчно на образце страницы для обоих языков. Не проверено вживую —
реальная отправка формы в браузере, реальный диалог с ботом.

**v37** — просмотрел не последние 10, а все 59 версий `CHANGELOG.md`
AELITA по SEO-кластеру (sitemap/robots.txt/canonical/alt/og:image), не
только структурные данные. Большинство находок за пределами v145–149
(последние 10) — это уже закрытые задачи AELITA, специфика её масштаба
(редиректы после переезда страницы, alt-тексты со сборочной машины,
`og:locale` между двумя партнёрскими сайтами) — у orgculture либо
такого нет архитектурно, либо уже чисто, проверено, не предположено:

- **Найден и исправлен реальный дубль контента** — `robots.txt`
  открывал `documents/privacy-policy.pdf`/`cookies-policy.pdf` для
  индексации, хотя оба PDF — тот же текст, что уже на `/privacy/` и
  `/cookies/` (HTML, с canonical/hreflang/OG, PDF — без ничего этого).
  Тот же класс бага, что AELITA чинила в `pack-v27`/`pack-v39`
  (`Disallow: /documents-pdf/`) — здесь точечно, только два файла
  (`Disallow: /documents/privacy-policy.pdf` и `.../cookies-policy.pdf`),
  не вся папка `documents/` — CV в ней уникален, HTML-двойника не
  имеет, индексация ему не мешает.
- **Проверено и подтверждено чистым** (не пропущено, а именно
  проверено): файлы `images/author.jpg`/`og-default.jpg`, на которые
  ссылается новый JSON-LD (v36) — существуют (аналог бага
  `pack-v145`/`pack-v146` с несуществующим `logo.png`); alt-тексты —
  без утечек путей сборочной машины (`pack-v20`); `sitemap.xml` —
  без конфликтов с `robots.txt` (`pack-v36` AELITA), `lastmod` свежий;
  `_redirect-for-online-domain` (отдельный домен-заглушка) правильно
  не в sitemap.
- **Не применимо архитектурно**: `og:locale`-рассинхрон между двумя
  сайтами (`pack-v148`) — у orgculture один сайт, не два партнёрских;
  переезд URL/редирект-страница после переименования (`pack-v6`) —
  адресов не переименовывали; `@id` вида `#event` на JSON-LD для
  будущего `aggregateRating` (`pack-v146`) — сайт осознанно отказался
  от звёздного рейтинга (см. `v34`), незачем готовить крепление под
  функцию, которую решили не делать.

`check_en.py` — чисто. `SITE_VERSION` 36→37 (только `robots.txt`
менялся — не генерируется скриптом, но версия и кэш-бастинг подняты
для консистентности с правилом «любая правка сайта — новая версия»).

**v36** — структурные данные (schema.org/JSON-LD), по прямому вопросу
про адаптацию под поисковые системы после переноса v35. Сверено, что
уже было, прежде чем что-то добавлять: `og:site_name`/полный набор
OG+Twitter Card уже стоял на всех 106 страницах через общий
`page_head()` (единая функция, не подушевное дублирование HTML, как
у AELITA до её же `aelita-v148`) — то, что AELITA чинила отдельным
заходом, здесь просто не могло разъехаться. JSON-LD уже был на всех
76 текстовых страницах (RU+EN, `Article` — заголовок, автор, издатель,
картинка) — не тронут. Не было нигде: на главной и на `/about`.

- **Главная (RU+EN)** — `@graph` из `WebSite` + `Person`, автор
  сайта — `Person` со стабильным `@id`, общим для RU и EN версий
  (одна и та же реальная персона, не два разных объекта).
- **`/about` (RU+EN)** — отдельный, более полный `Person`: `jobTitle`,
  `email`, `telephone`, `sameAs` (Telegram, VK, aelita-production.ru)
  — то же самое, что уже открыто написано текстом на самой странице,
  просто в машиночитаемом виде, ничего не добавлено сверх видимого.
  `@id` совпадает с `Person` на главной — оба блока описывают одну
  сущность.
- Не добавлено: `SearchAction` в `WebSite` — на сайте нет
  серверного поиска (клиентский полнотекстовый JS на `/texts/`), а
  `SearchAction` требует реального URL с параметром запроса; вписывать
  несуществующий эндпоинт — недостоверная разметка, хуже, чем никакой.
  Структурные данные на страницах проектов (`/projects/*`) — тоже не
  добавлены в этом заходе, отдельная задача (там уместнее
  `CreativeWork`/`Organization`, не `Person`, нужно решить, что именно).

Проверено: все 4 новых JSON-LD блока (`index.html`/`about/index.html`
RU+EN) распарсены `json.loads()` — валидны. `SITE_VERSION` 35→36,
сайт пересобран `gen.py`/`gen_en.py` целиком, `check_en.py` — чисто
(0 по всем четырём категориям).

**v35** — по итогам обзора пака AELITA (v124→v149), точечная правка,
не полный пересмотр (большая часть правок AELITA за этот диапазон —
билеты/оплата/личный кабинет/партнёрка/CRM, у сайта ничего из этого
нет, не переносилось). Взято одно: реальный, воспроизведённый баг из
`aelita-v125` — `.btn-outline` (здесь `.btn-line`) не сбрасывал
`background`, незаметно на `<a>` (фон и так прозрачный), но на
настоящем `<button>` браузер подставляет свой светлый фон под
`color:var(--ink)` (почти белый текст) — нечитаемый контраст.
Проверено, что баг реален и здесь, не гипотетически: `.btn-line`
используется как `<button>` на форме отзыва («Leave a review»/
«Send», добавлена в v34) на всех 76 текстовых страницах (RU+EN) —
единственное место на сайте, где класс применён к настоящей кнопке,
не к ссылке. `.filter-pill` (тоже кнопка) уже объявлял
`background:transparent` явно — не задет. Добавлено: `background:none`,
`-webkit-appearance:none`/`appearance:none`, `cursor:pointer`,
`font-family:inherit` — четыре свойства, которые не наследуются от
`<a>`-версии кнопки автоматически.

Заодно — `color-scheme:dark` в `:root` (у AELITA добавлен тем же
заходом по той же причине): сайт нигде не объявлял цветовую схему
явно, часть Android-браузеров форсирует тёмную тему эвристикой для
необъявленных страниц, что может добавочно ломать контраст
непредсказуемо поверх уже собственной тёмной палитры сайта.

Не перенесено из этого диапазона: аудит-скрипт синхронизации
футера (`aelita-v134`) — неприменим, футер здесь не дублируется
по страницам, единый источник в `gen.py`, разъехаться нечему;
инструмент точечной правки текста сайта коммитом в GitHub
(`aelita-v124`, `site-edit.js`) — не тривиальный перенос, отдельная
задача (нужны Cloud Function/токен/деплой, которых у сайта нет),
не сделано без отдельного запроса. Остальное (галереи,
программа чтения книжного клуба, страница QR-оплаты на площадке,
слияние с партнёром, формы бизнес-заявок) — специфика AELITA
как продакшена, не применимо к персональному сайту с текстами.

Собран (`gen.py`/`gen_en.py`), `check_en.py` — чисто (0 по всем
четырём категориям). `SITE_VERSION` 34→35, `sw.js`/sitemap/feed
пересобраны заодно тем же прогоном — не только `style.css`.

**v34** — по итогам обзора пака AELITA (v123): живая отдача отзывов и
форма на странице текста, без пересборки сайта. Взял идею у
`_tools/Reviews` AELITA (там Google-таблица вместо KV, но тот же
принцип — модерация даёт мгновенный результат, не ждёт следующего
деплоя), не звёздный рейтинг (конфликтует с голосом сайта — «не для
того, чтобы порекомендовать») и не Google Sheets вместо KV (лишний
внешний сервис ради данных, которые и так приходят раз в неделю в
Telegram).

- **Форма на странице текста** — рядом с кнопкой «через Telegram»,
  без Telegram вообще: имя (необязательно), текст, кнопка «Отправить».
  Honeypot-поле от ботов-спамеров. Ведёт к той же модерации, что и
  отзывы через бота.
- **Живая подгрузка** — JS на странице текста спрашивает у воркера
  `/reviews?slug=...`, что уже опубликовано в KV прямо сейчас, и
  дорисовывает то, чего ещё нет в статическом HTML. Отзыв виден на
  сайте сразу после «✅ Опубликовать» в боте — не нужно ждать
  пересборки/деплоя.
- **`WORKER_BASE`** — рефакторинг: раньше был отдельный `STATS_ENDPOINT`
  только для маячка статистики, теперь общая база, от которой строятся
  все три пути к воркеру (`/track`, `/reviews`, `/submit-review`).
- Протестировано в реальном DOM (jsdom) — живая подгрузка, открытие
  формы, валидация пустого текста, успешная отправка с проверкой
  тела запроса. Плюс сквозной смок-тест всего цикла (форма → бот →
  модерация → живая отдача) после `sync_texts.py`.

**v33** — 8 текстов, у которых раньше не было hero-картинки, получили
её (присылались по одной, сверялись перед подключением):
`shum-antishum-voronovskiy`, `mauzer-terzopulos`, `dyadya-vanya-tuminas`,
`krolik-dzhodzho`, `starshiy-syn`, `lyubimovka-pechenkin`,
`za-belym-krolikom`, `eyforiya`. Три из них (`mauzer-terzopulos`,
`dyadya-vanya-tuminas`, `krolik-dzhodzho`) заменялись по ходу — сначала
приходила версия меньше конвенции сайта (~1600px по ширине), затем
более крупная замена, старая версия файла перезаписывалась. Картинки
крупнее 1600px уменьшались под конвенцию (LANCZOS, quality 85-90),
картинки МЕНЬШЕ конвенции — не растягивались, сохранялись в родном
разрешении (растягивание только ухудшает чёткость): `dyadya-vanya-
tuminas.jpg` (800×533), `lyubimovka-pechenkin.jpg` (375×235,
заметно мельче остальных — если найдётся версия покрупнее, можно
заменить так же). Все размеры зарегистрированы в `IMAGE_DIMS`
(width/height у `<img>`, чтобы не прыгала вёрстка при загрузке).

**v32** — исправлены перепутанные фотографии у 8 текстов (по скриншотам,
присланным по одному, со сверкой перед правкой): картинки были
перемешаны единой цепочкой сдвига (не парами, а цепочкой из 7 звеньев),
восстановлен правильный порядок:
- `dvoe-na-kachelyah` ← `robot-kostya-1.jpg` (было `kacheli.jpg`)
- `robot-kostya` ← `annette-poster.jpg` (было `robot-kostya-1.jpg`)
- `dom-radio` ← без картинки (было `dom-radio.jpg`) — фото ушло на
  `medeya`, а на замену этому тексту в присланных скриншотах ничего не
  досталось; карточка на `/texts/` корректно откатывается на готовую
  заглушку (`thumb noimg`), сайт не ломается, но у текста сейчас нет
  hero-изображения — если найдётся подходящее фото, дай знать
- `kurentzis-znal-videl-chuvstvoval` ← `interview-v.jpg` (было
  `kurentzis-poster.jpg`)
- `annette` ← `medeya-poster.jpg` (было `annette-poster.jpg`)
- `medeya` ← `dom-radio.jpg` (было `medeya-poster.jpg`)
- `intervyu-v` ← `kacheli.jpg` (было `interview-v.jpg`)
- `net-nuzhdy-pisat` ← `kurentzis-poster.jpg` (раньше был без картинки
  вообще — не было `meta`-факта и hero-изображения ни у одной из версий)

Правка только в `TEXTS` (`gen.py`) — сами файлы в `images/` не
переименовывались и не трогались, поменялось только то, какой текст на
какой файл ссылается. Не задело EN-версию текстов отдельно (картинки
общие для RU/EN, как и раньше — правка сразу подхватилась в
`gen_en.py`).

**v31** — новая страница `/production/` — коммерческое предложение
вместо биографии за кнопкой «Продюсирование и продвижение»:
- Найден реальный баг: кнопка на главной вела на `about/#collab`, но
  на `/about/` никогда не было элемента с `id="collab"` — якорь
  физически ни на что не указывал (`collab_box()` рендерился только на
  `/projects/`). Открывался просто верх страницы с фото и биографией —
  то, что и вызвало вопрос «почему кнопка про услуги ведёт на фото».
- Новая `/production/` (+ `/en/production/`): что делаю (продюсирование
  / продвижение / сайты), для кого, примеры — 3 карточки реальных
  проектов (переиспользован `proj_card()`), «кто делает» — короткий
  блок со ссылкой на полную биографию на `/about/`, и `collab_box()` —
  теперь на странице, где `id="collab"` действительно существует.
  Биография — не отправная точка, а «отросток» страницы предложения,
  как и просили.
- Кнопка на главной (RU+EN) переключена на `production/`.
- Добавлено в sitemap (core_paths в `gen.py`, core в
  `build_combined_sitemap()` в `gen_en.py` — списки раздельные, оба
  обновлены).
- Новый CSS `.prod-services` (3 колонки на десктопе, 1 на мобильном —
  проверено скриншотом на ширине 390px).

**v30** — собственная статистика сайта и еженедельный отчёт в бота:
- Сайт (RU+EN) шлёт анонимный маячок о просмотре страницы на тот же
  Cloudflare Worker, что обслуживает бота (`own_stats_snippet()` в
  `gen.py`) — без cookie, без идентификатора пользователя, только
  путь/язык/источник перехода. Запускается тем же согласием, что и
  Яндекс.Метрика (кнопка «Хорошо» в cookie-баннере включает обе разом).
- Обновлены `privacy-policy` и `cookies-policy` — честно упоминают
  собственную статистику наравне с Яндекс.Метрикой.
- Бот (`04-bot/worker.js`) считает свою активность (/start, обращения,
  отзывы) в Cloudflare KV и раз в неделю (Cron Trigger, по умолчанию
  понедельник 9:00 UTC) сам присылает отчёт в личку — топ текстов,
  RU/EN разбивка, источники переходов, статистика бота.
- Оба механизма — опциональные, тот же принцип, что у `WEBHOOK_SECRET`
  и `GITHUB_TOKEN`: без настройки KV-хранилища сайт и бот работают как
  обычно, просто не будет отчёта. Настройка — `04-bot/README.md`,
  «Шаг 6».
- Проверено прогоном реальной логики `worker.js` с подменённым `fetch`
  и KV-хранилищем — маячки корректно пишутся в счётчики, `scheduled()`
  формирует отчёт (включая узнавание названия текста по slug) и не
  падает на пустых данных.

**v29** — фактчекинг всех 38 текстов, по прямому запросу, со сверкой
источников перед правкой. Найдены и исправлены 2 фактические ошибки
(RU+EN):
- «Ирландец» — 309 сцен снято на **117** локациях, не 170, как было
  написано (подтверждено Variety, ScreenRant и др.)
- «Любимовка» — драматург **Валерий Печейкин**, не «Печёнкин»
  (везде в источниках, включая его интервью и афишу Гоголь-центра,
  именно такое написание)

Остальные 36 текстов — имена, даты, названия постановок и площадок
проверены и совпадают с источниками (Александринский театр, Театр
им. Ленсовета, Вахтангова, Коляда-театр, Гоголь-центр, НИУ ВШЭ,
Complicité/Schaubühne и др.). Попутно уточнён «Новый Озон» текста —
удалось определить, что это «Посторонний» по Камю.

**v28** — «что стоит поправить» — сквозной аудит по запросу, не
только структурная проверка (`check_en.py` уже был чистым, но он не
ловит смысловые несостыковки):
- **`/about/` мета-описание** (то, что видно в Google/при шаринге,
  не видимый текст страницы) всё ещё говорило «продюсер, SMM, артист
  цирка» — тот же термин, что поправил в видимом тексте в v27, забыл
  поправить в мета. Теперь везде «продвижение».
- **Юзернейм бота дублировался в двух местах** (`BOT_USERNAME` для
  ссылок «Оставить отзыв» и отдельный литерал в `BOT_URL` для
  виджета «Написать боту» в футере) — если бы после реального запуска
  бота поправили один, а не другой, сайт бы разъехался сам с собой.
  `BOT_URL` теперь собирается из `BOT_USERNAME`, редактировать
  придётся только одно место.
- Заодно поправлен осиротевший обрывок комментария рядом с
  `YANDEX_METRIKA_ID` — часть исходного предложения потерялась при
  более ранней правке, читалось как не по-русски.
- Проверено и в порядке: sitemap.xml (52 записи) и feed.xml валидны;
  «SMM» в оставшихся 15 местах — это CV/история проектов на русском,
  осознанная конвенция («SMM» нормально в RU, не в EN), не бага.

**v27** — позиционирование за пределами главной: по итогам вопроса
«понятно ли в остальных местах, что я занимаюсь продюсированием» —
проверил весь сайт, не только hero-кнопку (правили её в v25):
- **Мета-описание главной** (RU и EN) — раньше было только про тексты
  ни слова о продюсировании/продвижении. Это самое заметное внешнее
  место: то, что видно в Google-поиске и при шаринге ссылки в
  Telegram/VK, часто первое, что видит человек до захода на сайт.
  Теперь честно называет обе стороны. Видимый текст hero на странице
  (`.lede`) не тронут — правка только в `<meta name="description">`/
  `og:description`.
- **`/about/`** — первый абзац био говорил «веду SMM» вместо
  «продвигаю», из-за чего расходился по формулировке с кнопкой на
  главной и текстом `/projects/`. Приведено к одному термину.
- Проверено и оставлено как есть: `/projects/` уже ясно говорит «то,
  чем занимаюсь параллельно с текстами»; футер и навигация — нейтральны,
  это нормально для этих мест.

**v26** — фактический контекст к текстам, из интернета:
- Новый `context.json` — год, режиссёр/автор, оригинальное название,
  короткий факт и ссылка на источник (Кинопоиск/Wikipedia/Meduza и
  др.) для 18 из 38 текстов — фильмы, спектакль, альбом, сериал.
  Собрано веб-поиском, не выдумано. Первый содержательный проход, не
  все 38 сразу — остальные дополнять тем же способом (см. комментарий
  в самом `context.json`).
- Новый блок «Контекст» на странице текста (RU и EN), CSS
  `.text-context`/`.ctx-*` — отдельно от авторского `meta`-поля в
  `TEXTS`: `meta` пишет и правит только Константин, `context.json` —
  фактическая справка, которую можно спокойно обновлять поиском, не
  трогая авторский текст.
- **Найдена и исправлена фактическая ошибка** в уже опубликованном
  тексте: «Ход королевы» вышел на Netflix 23 октября **2020** года, а
  не 2019, как было написано (и в RU, и в EN версии) — попутный
  результат проверки фактов через поиск, не специально искал ошибки.
- `director`/`fact`/`source_label` в `context.json` — на двух языках
  (`_en`-поля): это мой текст, не пользовательский контент вроде
  отзывов, поэтому переведён полностью, а не оставлен как есть. Для
  трёх фильмов без английского названия (Герда, Поклонница, Верность)
  и ещё двух (Сквозь чёрное стекло, Тот самый Мюнхгаузен) в EN-версии
  показывается уже устоявшийся перевод из `texts_en.json` — не
  придумывался заново, взят тот же, что и в самом тексте.
- `check_en.py` поймал 8 реальных замечаний при первой сборке (ёлочки
  и кириллица в EN-версии `context.json`) — все поправлены, финальная
  проверка чистая.

**v25** — позиционирование на главной: вторая кнопка в hero
(«Читать тексты» / …) переименована из «Для проектов» в
«Продюсирование и продвижение» (EN: «Production & promotion») — по
обратной связи о том, что «Для проектов» не читается как «здесь можно
заказать услугу», а сразу за кнопкой на `/about/#collab` есть чёткое
предложение услуг. Само содержимое `/about/#collab` (`collab_box()`)
не менялось — только вход к нему с главной. Из нескольких вариантов
(«Заказать продюсирование», «Продюсеру и площадкам» и др.) выбран
самый спокойный, без императива — в духе остального тона сайта («без
агрессии, но с экспрессией»).

**v24** — система отзывов на тексты:
- `reviews.json` — новый файл данных (slug → список отзывов), читает
  `gen.py` при каждой сборке. Не редактировать вручную поверх потока
  публикации через бота (см. `04-bot/README.md`, раздел «Отзывы на
  тексты») — публикация коммитит прямо в этот файл через GitHub API.
- Блок «Отзывы» на каждой странице текста (RU и EN) — уже
  опубликованные отзывы + кнопка «Оставить отзыв в Telegram»
  (deep-link `t.me/<BOT_USERNAME>?start=review_<slug>`, новая
  константа `BOT_USERNAME` в `gen.py`).
- Новые CSS-классы `.reviews-block`/`.review-item`/`.review-cta` в
  `assets/style.css`, в общей палитре сайта.

**v23** — перенесено обратно из работы над AELITA_pack: постоянный
QA-инструмент для английской версии.

При разборе перевода AELITA (отдельный проект, тот же автор) нашёл там
10 разрывов смысла, которые их собственный чекер `check_i18n.py`
пропускал раз за разом — потому что проверял полноту перевода
(«осталась ли кириллица»), а не связность. Оттуда же — идея самого
инструмента: 4 проверки, код возврата 1 при замечаниях, живёт в паке
постоянно, а не разовый скрипт в истории чата.

Для orgculture это не лечение (при вычитке v22 проверил вручную —
теги и повторяющиеся имена везде переведены единообразно), а
профилактика на будущее: если через полгода Константин попросит
добавить 39-й текст или поправить один старый, будет чем перепроверить
результат без перечитывания всех 38 текстов заново.

- **`check_en.py`** (новый) — 4 проверки: пропуски и полнота (все ли
  RU-тексты из `TEXTS` есть в `texts_en.json`, совпадает ли число
  абзацев — учит на чужой ошибке: 6 из 10 багов AELITA были именно
  обрезанным переводом при полном RU-ключе), канон имён, запрещённые
  паттерны (SMM, ёлочки), целостность (кириллица в видимом тексте,
  сбалансированность тегов, hreflang-пары в sitemap, битые ссылки).
  Запускать после любой правки: `python3 check_en.py`.
- **`glossary_en.json`** (новый) — канон терминов: `locked_terms`
  (Dom Radio, Currentzis, Chekhov, Butusov, Lensoviet Theatre,
  Alexandrinsky Theatre и ещё 20 имён/названий, которые повторяются
  в 2+ текстах), `tag_canon` (соответствие тегов), `forbidden_in_en`.
- **Найден и исправлен живой баг** первым же прогоном нового чекера:
  `aria-label` у переключателя языка на EN-страницах был на русском
  («Переключить на русский» вместо «Switch to Russian») — для
  скринридера на английской странице должно звучать по-английски.
  Правка в `gen.py` (общая функция `header()`), разошлась сразу на
  все 39 EN-страниц при пересборке.
- Тестировал сам чекер на намеренно испорченной копии
  `texts_en.json` (обрезал абзацы одного текста, сломал тег другого)
  — обе поломки поймал корректно, после отката снова чисто.

**v22** — вычитка английской версии под носителя языка:
Полный проход по всему английскому контенту с правкой калек и русского
синтаксиса. Смысл, структура, абзацы, ссылки и теги везде сохранены
1:1 с оригиналом — сверено программно после правки (совпадают slug'и,
количество абзацев, наличие meta, URL ссылок).

Что чинилось системно:
- **кальки-словосочетания**: «place the accents» → «decide where the
  emphasis falls», «charged people» → «running on real voltage»,
  «needless pathos» → «overwrought», «by my own sense» → «to my mind»,
  «writing for the drawer» → «writing for a desk drawer»;
- **ложные друзья**: русское «пафос» ≠ англ. pathos (в английском это
  «трогательность», а не «напыщенность») — заменено по смыслу;
  «агитка» → «party broadcast» вместо буквального «propaganda reel»;
- **русские усилители**: «catastrophically necessary/important» →
  «desperately important»; вычищено избыточное «genuinely / entirely /
  completely», которым добирался вес там, где в оригинале его давал
  порядок слов;
- **синтаксис**: развёрнуты русские причастные обороты и инверсии,
  которые в английском читаются как машинный перевод; длинные
  многосоставные предложения разбиты там, где английский требует точки;
- **пунктуация и стиль**: единый британский вариант (-ise, «programme»,
  «colour»), даты в формате «1 April 2021», кавычки-ёлочки убраны;
- **интерфейсные строки**: манифест, CV (буллеты), 5 карточек проектов,
  страница «Автор», collab-блок, подписи разделов, плейсхолдер поиска,
  тексты пустого результата — тоже переписаны, не только 38 текстов.

Отдельно: «SMM» в CV и карточках проектов заменён на «social media» —
аббревиатура распространена в русскоязычной среде, но в английском
резюме читается как непрофессиональный жаргон.

**v21** — RSS и PWA на английский:
- `en/feed.xml` — отдельная RSS-лента на английском (38 items из
  `TEXTS_EN`); `<link rel="alternate" type="application/rss+xml">` в
  `<head>` теперь ведёт на неё для EN-страниц (было: ссылки не было
  вообще — v20 сознательно её пропускала), и на `/en/texts/` появилась
  такая же ссылка «RSS →» под фильтром, как на русской `/texts/`;
- `en/manifest.json` — отдельный PWA-манифест: `name`/`short_name`/
  `description` на английском, `start_url`/`scope` = `/en/`, `lang="en"`.
  `page_head()` в `gen.py` теперь выбирает нужный manifest по `lang`
  (раньше все страницы, включая английские, ссылались на один и тот же
  русский `manifest.json`);
- `en/offline.html` — офлайн-страница на английском;
- `sw.js` **переписан заново в `gen_en.py`** (после `gen.py`) — теперь
  кэширует core-страницы обоих языков, оба manifest'а, обе offline-
  страницы, и при обрыве сети отдаёт офлайн-фолбэк на том языке, на
  котором был запрошенный путь (`/en/...` → `en/offline.html`, иначе →
  `offline.html`). **Важно**: `sw.js` больше не финальная версия сразу
  после `gen.py` — если запускать только `gen.py` без `gen_en.py`,
  сохранится урезанный RU-only service worker без английского раздела в
  precache-листе.
- Сайт поднят до `orgculture-v21`.

**v20** — полная английская версия сайта, `/en/`:
- новый `gen_en.py` — отдельный генератор, запускается после `gen.py`
  (переиспользует общую вёрстку и уже готовую в `gen.py` инфраструктуру
  под lang="en"/hreflang в `page_head`/`header`/`footer` — она была
  подготовлена заранее, но не использовалась; теперь задействована);
- переведены вручную все 38 текстов (`texts_en.json`, слоты slug/image/
  link общие с русским списком — переводился только видимый читателю
  текст), манифест, CV, все 5 карточек проектов, страница «Автор»,
  все три юридические страницы (privacy/cookies/bot-rules);
- переключатель языка (RU/EN) в шапке на каждой странице;
- `sitemap.xml` пересобирается заново после EN-сборки: теперь включает
  оба языка с `xhtml:link rel="alternate" hreflang`-парами на каждый URL;
- поиск по текстам (см. v19) продублирован на английской `/en/texts/` —
  свой индекс по переведённому тексту;
- RSS (`feed.xml`) и PWA (`manifest.json`/`sw.js`/`offline.html`)
  остаются общими на весь сайт, не дублируются на английский — см.
  «Что осознанно не сделано» ниже.
- **Важно**: переводы текстов — ручная работа, не сверялись построчно
  с оригиналом после генерации; при следующей правке стоит вычитать
  хотя бы часть текстов на естественность звучания.

**v19** — разделение аудитории на главной + поиск по текстам:
- под lede-абзацем hero-блока добавлены два CTA — «Читать тексты»
  (`/texts/`) и «Для проектов» (`/about/#collab`), чтобы читатель и
  потенциальный заказчик расходились по разным маршрутам уже с главной,
  а не смотрели один и тот же экран без разделения;
- у `collab-box` на странице «Автор» появился якорь `id="collab"`, чтобы
  вторая кнопка вела прямо к блоку «Сотрудничество», а не просто на
  верх страницы;
- на `/texts/` — клиентский полнотекстовый поиск: поле поверх фильтра
  по тегам, ищет по заголовку, кикеру, тегу и всем абзацам каждого
  текста. Индекс (`build_search_index()` в `gen.py`) встроен прямо в
  HTML страницы как `<script type="application/json">` — без бэкенда и
  без внешних запросов, работает и офлайн в PWA. Поиск и фильтр по тегу
  комбинируются (оба условия применяются одновременно).

**v18** — вторая партия технических правок по итогам разбора архива:
- у cookie-баннера появилась кнопка «Отклонить» (раньше был только
  «Хорошо» — фактически не давал выбора) и ссылка «Настройки cookie» в
  футере, которая сбрасывает согласие и снова показывает баннер (раньше
  отозвать согласие можно было только вручную через localStorage
  браузера);
- WEBHOOK_SECRET у Telegram-бота стал обязательным, как BOT_TOKEN и
  ADMIN_CHAT_ID (было опционально — без него кто угодно, узнавший URL
  воркера, мог слать поддельные апдейты);
- добавлены `og:url`, `og:site_name`, `og:locale` в `<head>`;
- `sitemap.xml` теперь пересобирается автоматически при каждом запуске
  `gen.py` (раньше был собран один раз вручную и не обновлялся бы при
  добавлении текста — старый TODO), плюс `<lastmod>` на каждый URL;
- `feed.xml` получил `<atom:link rel="self">` и `<lastBuildDate>`;
- все `<img>` получили атрибуты `width`/`height` (86 картинок) — раньше
  браузер не резервировал место под них и вёрстка прыгала при загрузке;
- `404.html` — добавлена вторая ссылка «К текстам» (была только «На
  главную»);
- ссылки на Instagram теперь помечены звёздочкой с пояснением, что Meta
  признана экстремистской организацией и запрещена в РФ (обязательная
  по закону пометка, раньше отсутствовала).

**v17** — технические правки по итогам разбора архива, без изменений
дизайна и вёрстки:
- исправлена опечатка «ZA VIST» → «ZAVIST'» в опыте на странице «Автор»
  (расходилась с написанием на странице «Проекты» и на komnata-sveta.ru);
- `gen.py` теперь запускается из любого расположения архива — раньше
  путь `ROOT` был захардкожен на `/home/claude/site` и скрипт падал
  сразу при попытке пересборки из распакованного архива;
- `SITE_VERSION` в предыдущей поставке (15) отставал от заявленной
  версии сайта (v16 из PWA) — синхронизирован;
- `gen_cookies_pdf.py` и `gen_privacy_pdf.py` больше не оставляют
  `cookies-src.html`/`privacy-src.html` в корне сайта — промежуточный
  HTML теперь пишется во временный файл ОС и удаляется после
  конвертации в PDF; старые оставшиеся файлы удалены из архива;
- добавлен `.nojekyll` в корень сайта (гигиена для GitHub Pages —
  отключает обработку Jekyll, которая сайту не нужна и может незаметно
  менять вывод для файлов/папок с ведущим подчёркиванием);
- заодно пересборка подтянула актуальный текст `privacy-policy.pdf` —
  версия в `07-documents/` была старой и не содержала пункт про
  Яндекс.Метрику и cookie-статистику, который уже есть в шаблоне и на
  странице `/privacy/`.

**v16** — сайт стал PWA (progressive web app), как и aelita-production.ru:
добавлены `manifest.json`, `sw.js` (service worker, network-first для HTML
/ cache-first для статики, тот же проверенный подход, что у Аэлиты) и
`offline.html`. Сайт можно «установить» на телефон, часть страниц
открывается без интернета после первого визита. Заодно добавлен
кэш-бастинг `style.css?v=16` — раньше правки CSS могли не
доходить до вернувшихся посетителей из-за агрессивного кэширования
браузером.

**v15** — на странице «Автор» появилась кнопка «Скачать CV»
(`documents/CV-Konstantin-Moshnikov.pdf`) — резюме в фирменном стиле
сайта, данные сверены с актуальной версией пака AELITA (pack-v6).

**v14** — у соглашения об использовании cookie появился PDF (`documents/cookies-policy.pdf`,
фирменное оформление — то же, что уже было у политики конфиденциальности)
+ кнопка «Скачать PDF» на странице `/cookies/`. Раньше cookie-документ
существовал только как HTML-страница, без файла для скачивания.

**v13** — правки по результатам разностороннего анализа: в конце каждого
текста теперь блок «Похожие тексты» (по тегу) + кнопка подписки на
Telegram-канал — раньше текст был тупиковым, читатель никуда не вёлся
дальше. Починен cookie-баннер: Метрика раньше запускалась сразу при
заходе, до согласия — теперь только после клика «Хорошо» (или если
согласие уже было дано в прошлый визит). Починена мёртвая ссылка на
афишу musicAeterna (устаревший поддомен `events.musicaeterna.org`
заменён на актуальный `musicaeterna.org/ru/visit/`).

**v12** — запас снизу у футера на мобильных, чтобы плавающая кнопка бота
не перекрывала ссылки на документы.

**v11** — навигация и техническое: кликабельный фильтр по тегам на
странице «Тексты», фавикон (SVG + PNG), per-page og:image из фото самого
текста (и переход на абсолютные URL — относительные пути соцсети
разворачивают некорректно), Schema.org Article, RSS-фид `/feed.xml`,
skip-to-content и рабочий `aria-expanded` на бургере.

**v10** — подключён реальный счётчик Яндекс.Метрики `111176053`.

**v9** — плавающая кнопка бота получила настоящую ссылку `@orgculture_bot`.

**v8** — cookie-баннер переделан под фирменный стиль: карточка с
терракотовой полосой и знаком вместо дефолтной полосы во всю ширину.

**v7** — подготовка к Метрике: код счётчика в шаблоне (выключен до
получения ID), страница `/cookies/`, cookie-баннер, упоминание Метрики
в политике конфиденциальности.

**v6** — Telegram-бот (код воркера + инструкция) и страница
`/bot-rules/`.

**v5** — PDF политики конфиденциальности в фирменном оформлении +
кнопка скачивания.

**v4** — страница `/privacy/` (политика обработки персональных данных
на самозанятого).

**v3** — раздел «Проекты»: каталог + 5 страниц проектов, блок
«Сотрудничество». Исправлена ошибка: БуФест относится к «Комнате
Света», а не к AELITA PRODUCTION. «Об авторе» переименован в «Автор».

**v2** — домен переехал на `orgculture.ru`, тексты выросли с 13 до 38
(canonical-ссылки и sitemap теперь считаются от реального пути, а не
всегда от корня), ссылки на Telegram/VK в футере, бегущая строка со
слоганом на главной, мобильная шапка свёрнута в бургер.

**v1** — первая версия: манифест, 13 текстов, рекомендации, об авторе.

---

**Домен: [orgculture.ru](https://orgculture.ru)** — основной, весь `gen.py`
и все канонические ссылки/sitemap уже прописаны под него (переменная
`SITE_DOMAIN` в начале `page_head()`).

Резервный **orgculture.online** должен только редиректить сюда — не
хостить копию сайта. Готовые конфиги для этого — в соседней папке
`site-online-redirect/` (см. её собственный README).

Статический сайт-архив текстов и манифеста «Организованной Культурности».
Чистый HTML/CSS, без сборки — что лежит в папке, то и деплоится (GitHub
Pages / Netlify / любой статический хостинг подойдут без изменений).

## Структура

```
/
├── index.html                 главная
├── manifesto/index.html       манифест (спецраздел)
├── texts/index.html           список всех текстов
├── texts/<slug>/index.html    каждый текст — своя папка (чистые адреса /texts/medeya и т.д.)
├── projects/index.html        каталог проектов
├── projects/<slug>/index.html 5 страниц проектов
├── recommendations/index.html подборка «что посмотреть / послушать»
├── about/index.html           автор + резюме
├── privacy/, cookies/, bot-rules/   юридические страницы
├── documents/                 PDF: политика, cookie, CV
├── assets/style.css           общие стили, дизайн-токены
├── assets/icons/              фавиконки и иконки PWA
├── images/                    фотографии и постеры к текстам
├── en/                         английская версия (зеркало той же структуры)
├── gen.py                     генератор русской версии сайта (всё, кроме 404)
├── gen_en.py                   генератор английской версии — запускать ПОСЛЕ gen.py
├── texts_en.json               переводы 38 текстов — источник данных для gen_en.py
├── glossary_en.json            канон имён/названий/тегов для EN — источник для check_en.py
├── reviews.json                 отзывы читателей — публикуются через бота (см. 04-bot/README.md), не редактировать вручную поверх потока
├── context.json                 фактическая справка к текстам (год/автор/факт/источник) — из веб-поиска, 18 из 38 текстов, дополнять так же
├── check_en.py                  проверка EN-версии — запускать после правок (см. ниже)
├── gen_cv_pdf.py, gen_privacy_pdf.py, gen_cookies_pdf.py   генераторы PDF
├── manifest.json, sw.js, offline.html   PWA
├── feed.xml                   RSS текстов
├── texts-manifest.json        список текстов для бота (см. 04-bot/sync_texts.py)
├── robots.txt, sitemap.xml
├── .nojekyll                  отключает обработку Jekyll на GitHub Pages
└── 404.html                   самодостаточная страница (свой <style>, без assets/style.css)
```

## Дизайн-токены (`assets/style.css`)

```css
--bg:     #0A0A0A   фон
--panel:  #131313   фон карточек
--line:   #262523   тонкие разделители
--ink:    #F5F2ED   текст, знак
--dim:    #9A968E   вторичный текст
--accent: #D97757   терракота — единственный акцент
```

Шрифты: **Unbounded** (заголовки, лёгкое начертание) и **Manrope** (текст),
подключены через Google Fonts. Сайт не будет выглядеть как задумано без
доступа в интернет на этапе загрузки — если понадобится офлайн-версия,
шрифты нужно будет захостить локально (`.woff2`).

## Логотип и написание — не шрифт, а векторная конструкция

Знак (эллипс О + К) и всё написание в hero-блоке главной страницы нарисованы
напрямую в SVG, встроены инлайн на каждой странице, где нужны (шапка сайта —
только знак, главная — знак + полное написание). Пропорция эллипса везде
одна: **rx 200 : ry 110**. Если нужно изменить толщину линии, размер или
пропорцию знака — правится в одном месте, `gen.py` (переменные
`LOGO_MARK_SVG` / `WORDMARK_DEFS`), сайт нужно пересобрать заново
(`python3 gen.py`), а не редактировать сгенерированные HTML-файлы вручную.

## `gen.py` — сайт генерируется, не пишется руками

Все 53 генерируемых HTML-файла собраны одним Python-скриптом (`gen.py`):
38 текстов + 5 страниц проектов + 10 остальных разделов (главная, тексты,
проекты, манифест, рекомендации, автор, политика, cookie, правила бота,
офлайн-страница). Собираются из:
- общих функций шапки/футера/карточки (`page_head`, `header`, `footer`, `card`),
- списка текстов `TEXTS` (заголовок, тег, кикер, абзацы, метаинформация, ссылка, картинка),
- списка пунктов манифеста `MANIFESTO_POINTS`,
- списка опыта `CV_ROLES` для страницы «Об авторе».

**Если нужно добавить/поправить текст** — редактируется словарь в `TEXTS`
внутри `gen.py`, затем `python3 gen.py` пересобирает весь сайт (текст,
индекс текстов, рекомендации и главную — они все ссылаются на один и тот же
список). Редактировать HTML в `texts/<slug>/index.html` напрямую не нужно —
при следующей пересборке правка потеряется.

**`texts-manifest.json`** — побочный файл, который `gen.py` пишет при
каждой сборке (список `slug` + `title` всех текстов). Это не часть
сайта, а единственный источник правды для списка текстов в
Telegram-боте — см. `04-bot/sync_texts.py`. **Если добавляешь текст —
после `gen.py` обязательно запускай ещё и `sync_texts.py` из `04-bot/`**,
иначе бот не узнает о новом тексте (команда «🎲 Случайный текст» его
просто никогда не покажет, без явной ошибки).

## Тексты: как устроены и что было сделано при переносе

Исходные тексты (посты из соцсетей) перенесены с лёгкой правкой: убраны
явные опечатки и тавтологии, добавлена единая структура (тег-категория,
короткий кикер под заголовком, блок с источником/датой/ссылкой внизу).
Голос и структура абзацев автора сохранены как есть — переписывался не
смысл, а шероховатости.

Ссылки на «где посмотреть/послушать» сохранены там, где были в оригинале
(Beat Film Festival, musicAeterna, Instagram «Робота Кости», Кинопоиск HD,
театр им. Ленсовета). Там, где в оригинальном посте ссылки не было
(«Аннетт», «Герда», «Ход королевы», «Высокое разрешение», «Майя
Плисецкая», «Интервью В», «Нет нужды писать») — она и не добавлена;
на странице рекомендаций для таких текстов кнопка ведёт на сам текст,
а не на внешний сервис.

## Бегущая строка (marquee)

На главной странице под hero-блоком — плакатная бегущая строка со слоганом,
приём в духе референса koroche.ru, но на своих цветах (терракотовый фон,
тёмный текст, шрифт Unbounded) и без заимствования их шрифта Archivo Black.
Компонент — функция `marquee()` в `gen.py`, включается/выключается одной
строкой в `build_index()`. Если приём приживётся — можно так же добавить
на страницы текстов или в шапку сайта целиком.

## Соцсети

Telegram (`t.me/orgculture`) и VK (`vk.ru/orgculture`) — в футере каждой
страницы и в контактах на странице «Об авторе».

## Изображения

`images/` — фотографии предоставлены автором (постеры фильмов, фото
спектаклей и концертов с уже проставленными кредитами фотографов —
не обрезайте подписи фотографов при дальнейшем редактировании фото).
Не у всех текстов есть фото — для текстов без изображения карточка и
хиро-блок используют фирменный узор (диагональная штриховка + знак ОК
полупрозрачно), а не пустое место или чужую стоковую картинку.

`images/og-default.jpg` (1200×630, знак на фирменном чёрном) — дефолтная
og:image/twitter:image для всех страниц, специфичного превью не задавали
отдельно ни у одной страницы.

## Что осознанно не сделано (в отличие от референса AELITA)

- **Нет форм обратной связи** — контакты (почта, соцсети) даются как
  обычные ссылки на странице «Об авторе».
- **Нет отдельных SEO-полей на каждую страницу** сверх title/description/
  og — при росте количества текстов имеет смысл добавить.
- **RSS и PWA не продублированы на английский** — сделано в v21, см.
  историю изменений выше.
- **Переводы 38 текстов** прошли отдельную вычитку в v22 (кальки,
  русский синтаксис, ложные друзья) — см. историю изменений. Это всё
  ещё перевод, а не авторский текст на английском: если английская
  версия станет рабочим инструментом (заявки, гранты, зарубежные
  партнёры), финальную проверку носителем языка стоит заложить
  отдельно.

## Проверка после правок

`sitemap.xml`, `feed.xml` и `texts-manifest.json` теперь пересобираются
автоматически при каждом запуске `gen.py` — вручную сверять их со
списком папок в `texts/` больше не нужно. **`sitemap.xml` пересобирается
ещё раз, уже с англ. hreflang-парами, внутри `gen_en.py`** — если
запускаешь только `gen.py` без `gen_en.py`, sitemap останется без
английских URL.

Что проверить после добавления текста:
1. `python3 gen.py` отработал без ошибок и написал все файлы;
2. добавлен перевод нового текста в `texts_en.json` (тот же slug), затем
   `python3 gen_en.py` — иначе английская версия останется без него;
3. запущен `python3 sync_texts.py` из `04-bot/` — иначе бот не узнает
   о новом тексте;
4. `SITE_VERSION` в `gen.py` и версия в шапке этого README подняты;
5. `BUILD_DATE` в `gen.py` обновлён — от него зависят `lastmod` в
   sitemap и `lastBuildDate` в RSS (в обеих лентах, `feed.xml` и
   `en/feed.xml`);
6. **`gen_en.py` запущен после `gen.py`, а не вместо него** — иначе
   `sitemap.xml` и `sw.js` останутся в урезанных RU-only версиях (оба
   файла `gen_en.py` пересобирает заново поверх того, что написал
   `gen.py`);
7. **`python3 check_en.py` — чисто.** Ловит то, что легко упустить при
   правке одного текста: перевод обрезан относительно оригинала, тег
   переведён не по канону, имя/название написано не так, как везде.
   Если добавлялось новое повторяющееся имя — стоит на всякий случай
   занести его в `glossary_en.json` (`locked_terms`), тогда проверка
   будет следить и за ним тоже.

## TODO на следующую версию

- Открытая форма подписки на новые тексты (email или Telegram-бот) —
  в исходных постах на неё была ссылка (vk.cc), на сайте пока нет
  аналога.
