# ОРГАНИЗОВАННАЯ КУЛЬТУРНОСТЬ — сайт

**Версия: `orgculture-v23`**
**Обновлён:** 06.08.2026

## Правило версий

При каждой партии изменений сайта обязательно:
1. Поднять номер версии выше
2. Обновить дату
3. Дописать строку в «Историю изменений» — что именно поменялось и почему
4. Обновить «Что осталось доделать», если что-то закрылось или появилось

Версия архива (`orgculture-archive-vN`) считается отдельно — см. README
в корне полного архива.

## SITE_VERSION — держать в синхроне с версией сайта

В `gen.py` есть отдельная константа `SITE_VERSION` — она отвечает за
кэш-бастинг `style.css?v=N` и за номер кэша в `sw.js`. **Бампать её
вместе с версией из этого README при каждой правке**, иначе вернувшиеся
пользователи будут долго видеть старые стили (service worker кэширует
статику по стратегии cache-first).

## История изменений

**v23** — перенесено обратно из работы над AELITA_pack: постоянный
QA-инструмент для английской версии.

При разборе перевода AELITA (отдельный проект, тот же автор) нашёл там
10 разрывов смысла, которые их собственный чекер `check_i18n.py`
пропускал раз за разом — потому что проверял полноту перевода
(«осталась ли кириллица»), а не связность. Оттуда же — идея самого
инструмента: 4 проверки, код возврата 1 при замечаниях, живёт в паке
постоянно, а не разовый скрипт в истории чата.

Для orgculture это не лечение (при вычитке v22 проверил вручную —
теги и повторяющиеся имена везде переведены единообразно), а
профилактика на будущее: если через полгода Константин попросит
добавить 39-й текст или поправить один старый, будет чем перепроверить
результат без перечитывания всех 38 текстов заново.

- **`check_en.py`** (новый) — 4 проверки: пропуски и полнота (все ли
  RU-тексты из `TEXTS` есть в `texts_en.json`, совпадает ли число
  абзацев — учит на чужой ошибке: 6 из 10 багов AELITA были именно
  обрезанным переводом при полном RU-ключе), канон имён, запрещённые
  паттерны (SMM, ёлочки), целостность (кириллица в видимом тексте,
  сбалансированность тегов, hreflang-пары в sitemap, битые ссылки).
  Запускать после любой правки: `python3 check_en.py`.
- **`glossary_en.json`** (новый) — канон терминов: `locked_terms`
  (Dom Radio, Currentzis, Chekhov, Butusov, Lensoviet Theatre,
  Alexandrinsky Theatre и ещё 20 имён/названий, которые повторяются
  в 2+ текстах), `tag_canon` (соответствие тегов), `forbidden_in_en`.
- **Найден и исправлен живой баг** первым же прогоном нового чекера:
  `aria-label` у переключателя языка на EN-страницах был на русском
  («Переключить на русский» вместо «Switch to Russian») — для
  скринридера на английской странице должно звучать по-английски.
  Правка в `gen.py` (общая функция `header()`), разошлась сразу на
  все 39 EN-страниц при пересборке.
- Тестировал сам чекер на намеренно испорченной копии
  `texts_en.json` (обрезал абзацы одного текста, сломал тег другого)
  — обе поломки поймал корректно, после отката снова чисто.

**v22** — вычитка английской версии под носителя языка:
Полный проход по всему английскому контенту с правкой калек и русского
синтаксиса. Смысл, структура, абзацы, ссылки и теги везде сохранены
1:1 с оригиналом — сверено программно после правки (совпадают slug'и,
количество абзацев, наличие meta, URL ссылок).

Что чинилось системно:
- **кальки-словосочетания**: «place the accents» → «decide where the
  emphasis falls», «charged people» → «running on real voltage»,
  «needless pathos» → «overwrought», «by my own sense» → «to my mind»,
  «writing for the drawer» → «writing for a desk drawer»;
- **ложные друзья**: русское «пафос» ≠ англ. pathos (в английском это
  «трогательность», а не «напыщенность») — заменено по смыслу;
  «агитка» → «party broadcast» вместо буквального «propaganda reel»;
- **русские усилители**: «catastrophically necessary/important» →
  «desperately important»; вычищено избыточное «genuinely / entirely /
  completely», которым добирался вес там, где в оригинале его давал
  порядок слов;
- **синтаксис**: развёрнуты русские причастные обороты и инверсии,
  которые в английском читаются как машинный перевод; длинные
  многосоставные предложения разбиты там, где английский требует точки;
- **пунктуация и стиль**: единый британский вариант (-ise, «programme»,
  «colour»), даты в формате «1 April 2021», кавычки-ёлочки убраны;
- **интерфейсные строки**: манифест, CV (буллеты), 5 карточек проектов,
  страница «Автор», collab-блок, подписи разделов, плейсхолдер поиска,
  тексты пустого результата — тоже переписаны, не только 38 текстов.

Отдельно: «SMM» в CV и карточках проектов заменён на «social media» —
аббревиатура распространена в русскоязычной среде, но в английском
резюме читается как непрофессиональный жаргон.

**v21** — RSS и PWA на английский:
- `en/feed.xml` — отдельная RSS-лента на английском (38 items из
  `TEXTS_EN`); `<link rel="alternate" type="application/rss+xml">` в
  `<head>` теперь ведёт на неё для EN-страниц (было: ссылки не было
  вообще — v20 сознательно её пропускала), и на `/en/texts/` появилась
  такая же ссылка «RSS →» под фильтром, как на русской `/texts/`;
- `en/manifest.json` — отдельный PWA-манифест: `name`/`short_name`/
  `description` на английском, `start_url`/`scope` = `/en/`, `lang="en"`.
  `page_head()` в `gen.py` теперь выбирает нужный manifest по `lang`
  (раньше все страницы, включая английские, ссылались на один и тот же
  русский `manifest.json`);
- `en/offline.html` — офлайн-страница на английском;
- `sw.js` **переписан заново в `gen_en.py`** (после `gen.py`) — теперь
  кэширует core-страницы обоих языков, оба manifest'а, обе offline-
  страницы, и при обрыве сети отдаёт офлайн-фолбэк на том языке, на
  котором был запрошенный путь (`/en/...` → `en/offline.html`, иначе →
  `offline.html`). **Важно**: `sw.js` больше не финальная версия сразу
  после `gen.py` — если запускать только `gen.py` без `gen_en.py`,
  сохранится урезанный RU-only service worker без английского раздела в
  precache-листе.
- Сайт поднят до `orgculture-v21`.

**v20** — полная английская версия сайта, `/en/`:
- новый `gen_en.py` — отдельный генератор, запускается после `gen.py`
  (переиспользует общую вёрстку и уже готовую в `gen.py` инфраструктуру
  под lang="en"/hreflang в `page_head`/`header`/`footer` — она была
  подготовлена заранее, но не использовалась; теперь задействована);
- переведены вручную все 38 текстов (`texts_en.json`, слоты slug/image/
  link общие с русским списком — переводился только видимый читателю
  текст), манифест, CV, все 5 карточек проектов, страница «Автор»,
  все три юридические страницы (privacy/cookies/bot-rules);
- переключатель языка (RU/EN) в шапке на каждой странице;
- `sitemap.xml` пересобирается заново после EN-сборки: теперь включает
  оба языка с `xhtml:link rel="alternate" hreflang`-парами на каждый URL;
- поиск по текстам (см. v19) продублирован на английской `/en/texts/` —
  свой индекс по переведённому тексту;
- RSS (`feed.xml`) и PWA (`manifest.json`/`sw.js`/`offline.html`)
  остаются общими на весь сайт, не дублируются на английский — см.
  «Что осознанно не сделано» ниже.
- **Важно**: переводы текстов — ручная работа, не сверялись построчно
  с оригиналом после генерации; при следующей правке стоит вычитать
  хотя бы часть текстов на естественность звучания.

**v19** — разделение аудитории на главной + поиск по текстам:
- под lede-абзацем hero-блока добавлены два CTA — «Читать тексты»
  (`/texts/`) и «Для проектов» (`/about/#collab`), чтобы читатель и
  потенциальный заказчик расходились по разным маршрутам уже с главной,
  а не смотрели один и тот же экран без разделения;
- у `collab-box` на странице «Автор» появился якорь `id="collab"`, чтобы
  вторая кнопка вела прямо к блоку «Сотрудничество», а не просто на
  верх страницы;
- на `/texts/` — клиентский полнотекстовый поиск: поле поверх фильтра
  по тегам, ищет по заголовку, кикеру, тегу и всем абзацам каждого
  текста. Индекс (`build_search_index()` в `gen.py`) встроен прямо в
  HTML страницы как `<script type="application/json">` — без бэкенда и
  без внешних запросов, работает и офлайн в PWA. Поиск и фильтр по тегу
  комбинируются (оба условия применяются одновременно).

**v18** — вторая партия технических правок по итогам разбора архива:
- у cookie-баннера появилась кнопка «Отклонить» (раньше был только
  «Хорошо» — фактически не давал выбора) и ссылка «Настройки cookie» в
  футере, которая сбрасывает согласие и снова показывает баннер (раньше
  отозвать согласие можно было только вручную через localStorage
  браузера);
- WEBHOOK_SECRET у Telegram-бота стал обязательным, как BOT_TOKEN и
  ADMIN_CHAT_ID (было опционально — без него кто угодно, узнавший URL
  воркера, мог слать поддельные апдейты);
- добавлены `og:url`, `og:site_name`, `og:locale` в `<head>`;
- `sitemap.xml` теперь пересобирается автоматически при каждом запуске
  `gen.py` (раньше был собран один раз вручную и не обновлялся бы при
  добавлении текста — старый TODO), плюс `<lastmod>` на каждый URL;
- `feed.xml` получил `<atom:link rel="self">` и `<lastBuildDate>`;
- все `<img>` получили атрибуты `width`/`height` (86 картинок) — раньше
  браузер не резервировал место под них и вёрстка прыгала при загрузке;
- `404.html` — добавлена вторая ссылка «К текстам» (была только «На
  главную»);
- ссылки на Instagram теперь помечены звёздочкой с пояснением, что Meta
  признана экстремистской организацией и запрещена в РФ (обязательная
  по закону пометка, раньше отсутствовала).

**v17** — технические правки по итогам разбора архива, без изменений
дизайна и вёрстки:
- исправлена опечатка «ZA VIST» → «ZAVIST'» в опыте на странице «Автор»
  (расходилась с написанием на странице «Проекты» и на komnata-sveta.ru);
- `gen.py` теперь запускается из любого расположения архива — раньше
  путь `ROOT` был захардкожен на `/home/claude/site` и скрипт падал
  сразу при попытке пересборки из распакованного архива;
- `SITE_VERSION` в предыдущей поставке (15) отставал от заявленной
  версии сайта (v16 из PWA) — синхронизирован;
- `gen_cookies_pdf.py` и `gen_privacy_pdf.py` больше не оставляют
  `cookies-src.html`/`privacy-src.html` в корне сайта — промежуточный
  HTML теперь пишется во временный файл ОС и удаляется после
  конвертации в PDF; старые оставшиеся файлы удалены из архива;
- добавлен `.nojekyll` в корень сайта (гигиена для GitHub Pages —
  отключает обработку Jekyll, которая сайту не нужна и может незаметно
  менять вывод для файлов/папок с ведущим подчёркиванием);
- заодно пересборка подтянула актуальный текст `privacy-policy.pdf` —
  версия в `07-documents/` была старой и не содержала пункт про
  Яндекс.Метрику и cookie-статистику, который уже есть в шаблоне и на
  странице `/privacy/`.

**v16** — сайт стал PWA (progressive web app), как и aelita-production.ru:
добавлены `manifest.json`, `sw.js` (service worker, network-first для HTML
/ cache-first для статики, тот же проверенный подход, что у Аэлиты) и
`offline.html`. Сайт можно «установить» на телефон, часть страниц
открывается без интернета после первого визита. Заодно добавлен
кэш-бастинг `style.css?v=16` — раньше правки CSS могли не
доходить до вернувшихся посетителей из-за агрессивного кэширования
браузером.

**v15** — на странице «Автор» появилась кнопка «Скачать CV»
(`documents/CV-Konstantin-Moshnikov.pdf`) — резюме в фирменном стиле
сайта, данные сверены с актуальной версией пака AELITA (pack-v6).

**v14** — у соглашения об использовании cookie появился PDF (`documents/cookies-policy.pdf`,
фирменное оформление — то же, что уже было у политики конфиденциальности)
+ кнопка «Скачать PDF» на странице `/cookies/`. Раньше cookie-документ
существовал только как HTML-страница, без файла для скачивания.

**v13** — правки по результатам разностороннего анализа: в конце каждого
текста теперь блок «Похожие тексты» (по тегу) + кнопка подписки на
Telegram-канал — раньше текст был тупиковым, читатель никуда не вёлся
дальше. Починен cookie-баннер: Метрика раньше запускалась сразу при
заходе, до согласия — теперь только после клика «Хорошо» (или если
согласие уже было дано в прошлый визит). Починена мёртвая ссылка на
афишу musicAeterna (устаревший поддомен `events.musicaeterna.org`
заменён на актуальный `musicaeterna.org/ru/visit/`).

**v12** — запас снизу у футера на мобильных, чтобы плавающая кнопка бота
не перекрывала ссылки на документы.

**v11** — навигация и техническое: кликабельный фильтр по тегам на
странице «Тексты», фавикон (SVG + PNG), per-page og:image из фото самого
текста (и переход на абсолютные URL — относительные пути соцсети
разворачивают некорректно), Schema.org Article, RSS-фид `/feed.xml`,
skip-to-content и рабочий `aria-expanded` на бургере.

**v10** — подключён реальный счётчик Яндекс.Метрики `111176053`.

**v9** — плавающая кнопка бота получила настоящую ссылку `@orgculture_bot`.

**v8** — cookie-баннер переделан под фирменный стиль: карточка с
терракотовой полосой и знаком вместо дефолтной полосы во всю ширину.

**v7** — подготовка к Метрике: код счётчика в шаблоне (выключен до
получения ID), страница `/cookies/`, cookie-баннер, упоминание Метрики
в политике конфиденциальности.

**v6** — Telegram-бот (код воркера + инструкция) и страница
`/bot-rules/`.

**v5** — PDF политики конфиденциальности в фирменном оформлении +
кнопка скачивания.

**v4** — страница `/privacy/` (политика обработки персональных данных
на самозанятого).

**v3** — раздел «Проекты»: каталог + 5 страниц проектов, блок
«Сотрудничество». Исправлена ошибка: БуФест относится к «Комнате
Света», а не к AELITA PRODUCTION. «Об авторе» переименован в «Автор».

**v2** — домен переехал на `orgculture.ru`, тексты выросли с 13 до 38
(canonical-ссылки и sitemap теперь считаются от реального пути, а не
всегда от корня), ссылки на Telegram/VK в футере, бегущая строка со
слоганом на главной, мобильная шапка свёрнута в бургер.

**v1** — первая версия: манифест, 13 текстов, рекомендации, об авторе.

---

**Домен: [orgculture.ru](https://orgculture.ru)** — основной, весь `gen.py`
и все канонические ссылки/sitemap уже прописаны под него (переменная
`SITE_DOMAIN` в начале `page_head()`).

Резервный **orgculture.online** должен только редиректить сюда — не
хостить копию сайта. Готовые конфиги для этого — в соседней папке
`site-online-redirect/` (см. её собственный README).

Статический сайт-архив текстов и манифеста «Организованной Культурности».
Чистый HTML/CSS, без сборки — что лежит в папке, то и деплоится (GitHub
Pages / Netlify / любой статический хостинг подойдут без изменений).

## Структура

```
/
├── index.html                 главная
├── manifesto/index.html       манифест (спецраздел)
├── texts/index.html           список всех текстов
├── texts/<slug>/index.html    каждый текст — своя папка (чистые адреса /texts/medeya и т.д.)
├── projects/index.html        каталог проектов
├── projects/<slug>/index.html 5 страниц проектов
├── recommendations/index.html подборка «что посмотреть / послушать»
├── about/index.html           автор + резюме
├── privacy/, cookies/, bot-rules/   юридические страницы
├── documents/                 PDF: политика, cookie, CV
├── assets/style.css           общие стили, дизайн-токены
├── assets/icons/              фавиконки и иконки PWA
├── images/                    фотографии и постеры к текстам
├── en/                         английская версия (зеркало той же структуры)
├── gen.py                     генератор русской версии сайта (всё, кроме 404)
├── gen_en.py                   генератор английской версии — запускать ПОСЛЕ gen.py
├── texts_en.json               переводы 38 текстов — источник данных для gen_en.py
├── glossary_en.json            канон имён/названий/тегов для EN — источник для check_en.py
├── check_en.py                  проверка EN-версии — запускать после правок (см. ниже)
├── gen_cv_pdf.py, gen_privacy_pdf.py, gen_cookies_pdf.py   генераторы PDF
├── manifest.json, sw.js, offline.html   PWA
├── feed.xml                   RSS текстов
├── texts-manifest.json        список текстов для бота (см. 04-bot/sync_texts.py)
├── robots.txt, sitemap.xml
├── .nojekyll                  отключает обработку Jekyll на GitHub Pages
└── 404.html                   самодостаточная страница (свой <style>, без assets/style.css)
```

## Дизайн-токены (`assets/style.css`)

```css
--bg:     #0A0A0A   фон
--panel:  #131313   фон карточек
--line:   #262523   тонкие разделители
--ink:    #F5F2ED   текст, знак
--dim:    #9A968E   вторичный текст
--accent: #D97757   терракота — единственный акцент
```

Шрифты: **Unbounded** (заголовки, лёгкое начертание) и **Manrope** (текст),
подключены через Google Fonts. Сайт не будет выглядеть как задумано без
доступа в интернет на этапе загрузки — если понадобится офлайн-версия,
шрифты нужно будет захостить локально (`.woff2`).

## Логотип и написание — не шрифт, а векторная конструкция

Знак (эллипс О + К) и всё написание в hero-блоке главной страницы нарисованы
напрямую в SVG, встроены инлайн на каждой странице, где нужны (шапка сайта —
только знак, главная — знак + полное написание). Пропорция эллипса везде
одна: **rx 200 : ry 110**. Если нужно изменить толщину линии, размер или
пропорцию знака — правится в одном месте, `gen.py` (переменные
`LOGO_MARK_SVG` / `WORDMARK_DEFS`), сайт нужно пересобрать заново
(`python3 gen.py`), а не редактировать сгенерированные HTML-файлы вручную.

## `gen.py` — сайт генерируется, не пишется руками

Все 53 генерируемых HTML-файла собраны одним Python-скриптом (`gen.py`):
38 текстов + 5 страниц проектов + 10 остальных разделов (главная, тексты,
проекты, манифест, рекомендации, автор, политика, cookie, правила бота,
офлайн-страница). Собираются из:
- общих функций шапки/футера/карточки (`page_head`, `header`, `footer`, `card`),
- списка текстов `TEXTS` (заголовок, тег, кикер, абзацы, метаинформация, ссылка, картинка),
- списка пунктов манифеста `MANIFESTO_POINTS`,
- списка опыта `CV_ROLES` для страницы «Об авторе».

**Если нужно добавить/поправить текст** — редактируется словарь в `TEXTS`
внутри `gen.py`, затем `python3 gen.py` пересобирает весь сайт (текст,
индекс текстов, рекомендации и главную — они все ссылаются на один и тот же
список). Редактировать HTML в `texts/<slug>/index.html` напрямую не нужно —
при следующей пересборке правка потеряется.

**`texts-manifest.json`** — побочный файл, который `gen.py` пишет при
каждой сборке (список `slug` + `title` всех текстов). Это не часть
сайта, а единственный источник правды для списка текстов в
Telegram-боте — см. `04-bot/sync_texts.py`. **Если добавляешь текст —
после `gen.py` обязательно запускай ещё и `sync_texts.py` из `04-bot/`**,
иначе бот не узнает о новом тексте (команда «🎲 Случайный текст» его
просто никогда не покажет, без явной ошибки).

## Тексты: как устроены и что было сделано при переносе

Исходные тексты (посты из соцсетей) перенесены с лёгкой правкой: убраны
явные опечатки и тавтологии, добавлена единая структура (тег-категория,
короткий кикер под заголовком, блок с источником/датой/ссылкой внизу).
Голос и структура абзацев автора сохранены как есть — переписывался не
смысл, а шероховатости.

Ссылки на «где посмотреть/послушать» сохранены там, где были в оригинале
(Beat Film Festival, musicAeterna, Instagram «Робота Кости», Кинопоиск HD,
театр им. Ленсовета). Там, где в оригинальном посте ссылки не было
(«Аннетт», «Герда», «Ход королевы», «Высокое разрешение», «Майя
Плисецкая», «Интервью В», «Нет нужды писать») — она и не добавлена;
на странице рекомендаций для таких текстов кнопка ведёт на сам текст,
а не на внешний сервис.

## Бегущая строка (marquee)

На главной странице под hero-блоком — плакатная бегущая строка со слоганом,
приём в духе референса koroche.ru, но на своих цветах (терракотовый фон,
тёмный текст, шрифт Unbounded) и без заимствования их шрифта Archivo Black.
Компонент — функция `marquee()` в `gen.py`, включается/выключается одной
строкой в `build_index()`. Если приём приживётся — можно так же добавить
на страницы текстов или в шапку сайта целиком.

## Соцсети

Telegram (`t.me/orgculture`) и VK (`vk.ru/orgculture`) — в футере каждой
страницы и в контактах на странице «Об авторе».

## Изображения

`images/` — фотографии предоставлены автором (постеры фильмов, фото
спектаклей и концертов с уже проставленными кредитами фотографов —
не обрезайте подписи фотографов при дальнейшем редактировании фото).
Не у всех текстов есть фото — для текстов без изображения карточка и
хиро-блок используют фирменный узор (диагональная штриховка + знак ОК
полупрозрачно), а не пустое место или чужую стоковую картинку.

`images/og-default.jpg` (1200×630, знак на фирменном чёрном) — дефолтная
og:image/twitter:image для всех страниц, специфичного превью не задавали
отдельно ни у одной страницы.

## Что осознанно не сделано (в отличие от референса AELITA)

- **Нет форм обратной связи** — контакты (почта, соцсети) даются как
  обычные ссылки на странице «Об авторе».
- **Нет отдельных SEO-полей на каждую страницу** сверх title/description/
  og — при росте количества текстов имеет смысл добавить.
- **RSS и PWA не продублированы на английский** — сделано в v21, см.
  историю изменений выше.
- **Переводы 38 текстов** прошли отдельную вычитку в v22 (кальки,
  русский синтаксис, ложные друзья) — см. историю изменений. Это всё
  ещё перевод, а не авторский текст на английском: если английская
  версия станет рабочим инструментом (заявки, гранты, зарубежные
  партнёры), финальную проверку носителем языка стоит заложить
  отдельно.

## Проверка после правок

`sitemap.xml`, `feed.xml` и `texts-manifest.json` теперь пересобираются
автоматически при каждом запуске `gen.py` — вручную сверять их со
списком папок в `texts/` больше не нужно. **`sitemap.xml` пересобирается
ещё раз, уже с англ. hreflang-парами, внутри `gen_en.py`** — если
запускаешь только `gen.py` без `gen_en.py`, sitemap останется без
английских URL.

Что проверить после добавления текста:
1. `python3 gen.py` отработал без ошибок и написал все файлы;
2. добавлен перевод нового текста в `texts_en.json` (тот же slug), затем
   `python3 gen_en.py` — иначе английская версия останется без него;
3. запущен `python3 sync_texts.py` из `04-bot/` — иначе бот не узнает
   о новом тексте;
4. `SITE_VERSION` в `gen.py` и версия в шапке этого README подняты;
5. `BUILD_DATE` в `gen.py` обновлён — от него зависят `lastmod` в
   sitemap и `lastBuildDate` в RSS (в обеих лентах, `feed.xml` и
   `en/feed.xml`);
6. **`gen_en.py` запущен после `gen.py`, а не вместо него** — иначе
   `sitemap.xml` и `sw.js` останутся в урезанных RU-only версиях (оба
   файла `gen_en.py` пересобирает заново поверх того, что написал
   `gen.py`);
7. **`python3 check_en.py` — чисто.** Ловит то, что легко упустить при
   правке одного текста: перевод обрезан относительно оригинала, тег
   переведён не по канону, имя/название написано не так, как везде.
   Если добавлялось новое повторяющееся имя — стоит на всякий случай
   занести его в `glossary_en.json` (`locked_terms`), тогда проверка
   будет следить и за ним тоже.

## TODO на следующую версию

- Открытая форма подписки на новые тексты (email или Telegram-бот) —
  в исходных постах на неё была ссылка (vk.cc), на сайте пока нет
  аналога.
